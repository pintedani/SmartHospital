{
  "buildCommand": "cd src/SmartHospital.Web && npm install && npm run build",
  "outputDirectory": "src/SmartHospital.Web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
